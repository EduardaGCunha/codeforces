#include <bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(0); cin.tie(0)
#define int long long
#define endl '\n'

/*
what is the biggest tower we can teleport to?
how can we discover that?
it will be the biggest tower that has the minimum difference of i from us 
as long as it doesnt take more time than it takes for x to arrive at our current position
*/

void solve(){
	
}

signed main(){
	fastio;
	int t; cin >> t;
	while(t--){
		solve();
	}
}